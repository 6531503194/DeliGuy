server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/auth_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  kafka:
    bootstrap-servers: kafka:9092
